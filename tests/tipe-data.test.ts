describe("Data Type", () => {
    it("should must declare", () => {
        let name: string = "Husnul";
        let balance: number = 1000;
        let isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);

        // name = 100;
        // balance = "Husnul";
        // isVip = 100;
    });
});
